# scripts/content

Reserved for content-production/development tooling (for example the CC-10
AI content pipeline proof: candidate generation, independent verification,
schema validation against `@alp/content-schema`).

No scripts exist yet. This directory is tracked now so the repository
foundation reflects the approved package/workspace boundaries; scripts are
added when the owning CC package is implemented. Anything added here is
development/content tooling only and must never be imported by the
learner-runtime domain engines.
