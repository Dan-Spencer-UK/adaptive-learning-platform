---
id: SEC-002
status: approved
owner: project-architect
last_reviewed: 2026-08-15
---

# Security Verification Matrix

This maps applicable security requirements to project controls/evidence. It begins as a scaffold and is populated during implementation. An incomplete row is not compliance.

Status values: NOT_ASSESSED, APPLICABLE, NOT_APPLICABLE, IMPLEMENTED, VERIFIED, EXCEPTION.

| ID | Standard / version | Requirement area | Applicability | Project control | Verification / evidence | Status | Exception / rationale |
|---|---|---|---|---|---|---|---|
| SEC-M-001 | OWASP ASVS 5.0.0 | Authentication | Applicable | Supabase Auth passwordless email one-time-code (local Mailpit mail catcher only); short-lived/single-use OTP (`supabase/config.toml` `[auth.email]` `otp_length`/`otp_expiry`); no custom auth cryptography; server-side session verified via `auth.getClaims()` (JWT signature verification), never trusted from an unverified client object; logout terminates the session server-side via `auth.signOut()`, not merely client UI state | pgTAP `supabase/tests/database/08_learner_isolation.sql`; Playwright `tests/e2e/sign-in.spec.ts` (full email-OTP round trip via the local Mailpit API, and a session-revoked-after-sign-out assertion); `npm run test:e2e` | VERIFIED | Production auth email (Resend/custom SMTP), administrator MFA and auth abuse/rate-limit hardening beyond Supabase Auth's local defaults remain required before external beta (CC-14 scope). Social/OAuth providers are not implemented (out of CC-03 scope). |
| SEC-M-002 | OWASP ASVS 5.0.0 | Access control | Applicable | Server-side authorisation at two independent layers: `proxy.ts` request interceptor redirects unauthenticated requests away from `/learn`, and the `/learn` page independently re-verifies via `auth.getClaims()` rather than trusting the interceptor alone; PostgreSQL RLS on `learner_profiles`/`learner_isolation_probe` (self-only SELECT/INSERT/UPDATE; anon denied; no `USING (true)`/`WITH CHECK (true)` policy) | pgTAP `supabase/tests/database/08_learner_isolation.sql` (19 assertions: User A/User B cross-read/update/insert-ownership denial, anonymous denial, idempotent duplicate-profile-insert proof (CC-03A), governed CC-02 tables confirmed unaffected by the new grants); Playwright `tests/e2e/sign-in.spec.ts` (unauthenticated `/learn` access redirected to sign-in); `npm run db:test` / `npm run test:e2e` | VERIFIED | Executable cross-user isolation evidence (User A / User B pattern) now exists for the CC-03 learner-owned tables. Broader learner-data tables (evidence, mastery, attempts) do not exist yet and require their own RLS verification when introduced in later CC packages. |
| SEC-M-003 | OWASP ASVS 5.0.0 | Input validation | Applicable | Zod + DB constraints | Unit/integration tests | NOT_ASSESSED | |
| SEC-M-004 | OWASP ASVS 5.0.0 | Error handling/logging | Applicable | Safe errors + structured logs | Test/review | NOT_ASSESSED | |
| SEC-M-005 | OWASP ASVS 5.0.0 | Data protection | Applicable | TLS/provider controls + least privilege/minimisation | Config review | NOT_ASSESSED | |
| SEC-M-006 | NIST SSDF 1.1 | Secure development | Applicable | Bounded tasks, reviews, CI, scans | Repository/CI evidence | NOT_ASSESSED | |
| SEC-M-007 | OWASP Top 10:2025 | Risk cross-check | Applicable | Threat/risk review against implemented slice | Review evidence | NOT_ASSESSED | |
| SEC-M-008 | CIS | PostgreSQL/managed hardening | Assess | Supabase-configurable controls | Applicability review | NOT_ASSESSED | |
| SEC-M-009 | CIS | Hosting/platform hardening | Assess | Vercel/Supabase tenant controls | Applicability review | NOT_ASSESSED | |
| SEC-M-002a | OWASP ASVS 5.0.0 | Access control — deny-by-default baseline (pre-authentication) | Applicable | PostgreSQL RLS enabled with zero policies, plus explicit `REVOKE ALL` from `anon`/`authenticated`, on every CC-02 governed knowledge/provenance/curriculum table, including `assertion_versions` (`supabase/migrations/20260814120200_rls_baseline.sql`); CC-04 extended the same posture to the new `misconceptions`/`misconception_assertion_conflicts` tables (`supabase/migrations/20260815090100_misconception_rls_and_grants.sql`) | pgTAP `supabase/tests/database/04_rls_baseline.sql` (19 assertions: RLS enabled per table, zero policies exist, anon/authenticated read+write denied with 42501); `supabase/tests/database/09_misconception_schema.sql` (15 assertions: same posture for the CC-04 misconception tables) and `10_unit202_knowledge_graph.sql` (41 assertions: confirms `assertions`/`misconceptions` remain denied to anon/authenticated after CC-04B's expanded 176-assertion curriculum-grounded corpus, 146 of which are Electrical, was added); `npm run db:test`; CI `database` job | VERIFIED | This row covers the CC-02/CC-04 governed knowledge/provenance/curriculum/misconception tables, which remain deliberately policy-free. Cross-user learner isolation now has executable evidence for the CC-03 learner-owned tables -- see SEC-M-002. |
| SEC-M-003a | OWASP ASVS 5.0.0 | Input validation — database constraints | Applicable | CHECK constraints constraining rights classification, relationship/mapping/provenance-role enums and lifecycle status; NOT NULL/UNIQUE/FK constraints on all CC-02 tables; self-link prevention on assertion relationships and curriculum-node parents; composite FK enforcing assertion-version uniqueness per identity and curriculum-node parent/child same-version consistency; `assertion_provenance_links` FKs to the specific `assertion_versions.id` it supports (not stable assertion identity), so relationships/mappings (stable identity) and provenance (specific version) are enforced at different, correctly-scoped FK targets. CC-04A added a manifest-level (pre-SQL) validation layer in `@alp/content-schema`'s `knowledgeGraphManifestSchema` (`packages/content-schema/src/knowledge-graph.ts`): duplicate stable-identifier detection, unresolved cross-reference detection, an APPROVED/PUBLISHED-assertion-without-provenance guard, and a guard rejecting a CURRENT source/curriculum version whose own edition/version label admits it is an unconfirmed placeholder (the exact defect CC-04A corrected) | pgTAP `supabase/tests/database/00_schema.sql`, `01_rights_classification.sql`, `02_provenance_and_mapping.sql`, `03_assertion_relationships.sql`, `05_assertion_versioning.sql`, `06_curriculum_hierarchy_integrity.sql`; Vitest `packages/content-schema/src/knowledge-graph.test.ts` (16 tests, including the duplicate-identifier, missing-provenance and placeholder-CURRENT-label guards); `npm run db:test` / `npm run test:unit` | VERIFIED | UNKNOWN is deliberately excluded from the rights-classification CHECK constraint so it cannot be recorded as a learner-reproduction rights class. |
| SEC-M-006a | NIST SSDF 1.1 | Reproducible database migrations | Applicable | Version-controlled SQL migrations under `supabase/migrations/`; no schema state exists only in a running database | Manual verification: `npx supabase db reset` rebuilds schema + `supabase/seed.sql` from repository state alone with no manual Studio step (verified 2026-08-14); CI `database` job runs the same reset on every push/PR | VERIFIED | |
| SEC-M-003b | OWASP ASVS 5.0.0 | Input validation — sign-in form | Applicable | Server-side email/OTP-code format validation (`apps/web/lib/sign-in-validation.ts`), independent of the HTML5 `type="email"`/client validation; generic anti-enumeration error/message wording that does not reveal whether an account exists; `next` redirect parameter constrained to an internal relative path (`apps/web/lib/safe-redirect.ts`), never an arbitrary external URL | Vitest `apps/web/lib/sign-in-validation.test.ts`; component tests `apps/web/app/sign-in/sign-in-form.test.tsx`; `npm run test:unit` | VERIFIED | Plain regex validation was used rather than Zod (approved but not yet added to the repository); the fields involved (email, 6-digit code) do not warrant introducing the dependency yet. |
| SEC-M-010 | OWASP ASVS 5.0.0 | Secrets — native mobile client | Applicable | CC-04N `apps/mobile` uses only the public/publishable Supabase key (`EXPO_PUBLIC_SUPABASE_URL`/`EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, bundled-into-client by Expo's own convention, the mobile equivalent of the web client's `NEXT_PUBLIC_*` trust boundary); no service-role key, database credential or other privileged secret exists anywhere in `apps/mobile` source or config. `docs/security/SECURITY-BASELINE.md`'s "never expose... to browser bundles" rule was generalised in CC-04M to cover any client bundle | Manual repository review of `apps/mobile/src` and `apps/mobile/.env.example` (grep for `service_role`/`SECRET`/`JWT_SECRET`: no matches); `npm run check:mobile-boundary` (unrelated but adjacent automated boundary control, see SEC-M-012) | VERIFIED | |
| SEC-M-011 | OWASP ASVS 5.0.0 | Session management — native mobile client | Applicable | Native session persistence uses `LargeSecureStore` (`apps/mobile/src/lib/supabase/large-secure-store.ts`), Supabase's own documented Expo/React Native pattern (verified against current official Supabase docs 2026-08-15): a random AES-256 key held in `expo-secure-store` (OS Keychain/Keystore), encrypting the session payload stored in AsyncStorage (because SecureStore alone cannot hold a value >2048 bytes). No custom/bespoke cryptography was introduced -- this is the documented upstream pattern, reproduced faithfully. `AppState`-driven `startAutoRefresh`/`stopAutoRefresh` registered exactly once, per Supabase guidance. No session value is logged | Manual verification against current Supabase documentation (cited in `docs/architecture/adr/ADR-0001-mobile-client-technology.md` and `MOBILE-ARCHITECTURE.md` §3); code review of `apps/mobile/src/lib/supabase/large-secure-store.ts` and `client.ts`; `npm run mobile:test` (component-level sign-in flow test, `apps/mobile/src/app/__tests__/sign-in.test.tsx`) | VERIFIED | Real device Keychain/Keystore behaviour (as opposed to the code path being correct) remains real-device/emulator qualification, consistent with the rest of CC-04N's hardware-dependent items -- see `docs/architecture/evidence/CC-04N-MOBILE-FOUNDATION-EVIDENCE.md`. |
| SEC-M-012 | N/A (architecture/dependency-isolation control) | Mobile/web UI package boundary | Applicable | `packages/ui` (web/DOM-specific, per `docs/architecture/MOBILE-ARCHITECTURE.md` §1) must never be imported by `apps/mobile`. Enforced by a mechanical guard (`scripts/boundary-checks/check-mobile-web-boundary.ts`) checking both `apps/mobile/package.json`'s declared dependencies and every source file under `apps/mobile/src` for `@alp/ui`/`packages/ui` references | `npm run check:mobile-boundary`; wired into CI's `checks` job (`.github/workflows/ci.yml`) | VERIFIED | Not a learner-data security boundary in the ASVS sense; recorded here because it is a genuinely new automated control introduced by CC-04N and belongs in the same evidence trail. |
| SEC-M-006b | NIST SSDF 1.1 / dependency audit | Known upstream dependency vulnerability -- `image-size` (HIGH x2, DoS) | Applicable | `image-size@1.2.1` (pulled transitively via `metro@0.84.4` <- `@expo/metro@56.0.0` <- `expo@57.0.14` -- Expo SDK 57's own toolchain, not a package this repository depends on directly) is affected by two independent GHSA advisories: `GHSA-w3rx-r6r6-pgpr` (ICNS parser infinite-loop DoS, CVSS 3.1 7.5, CWE-835) and `GHSA-5p2g-fcmc-qvqq` (JXL/HEIF parser infinite-loop DoS, same CVSS/CWE). **Correction to an earlier record**: `image-size@2.0.2` is **not** a patched release -- both GHSA advisories list "Patched versions: None" and 2.0.2 (npm's current `latest` tag) remains within the vulnerable `<=2.0.2` range for both. Metro's own maintainers attempted three separate migrations to a maintained fork (`image-size-next`, PRs #1859/#1853/#1852, GitHub) and all were abandoned; every Metro release through the current `0.87.0` still declares `image-size@^1.0.2`. `npm audit fix --force` would downgrade `react-native` to `0.72.17`, violating the accepted `ADR-0001` baseline -- rejected. An `overrides` pin to `image-size@2.0.2` was tested and reverted (both because `npm ls` reported it `invalid` against Metro's declared range, and because 2.0.2 would not have fixed either advisory anyway). No compatible upstream fix exists in the accepted Expo SDK 57 / React Native 0.86 line as of 2026-08-15 -- Options A (SDK-57-line patch) and B (supported Metro/dependency fix) were both investigated and are genuinely unavailable, not merely inconvenient. | A governed audit gate, `scripts/security/check-npm-audit.mjs`, parses real `npm audit --json` output and extracts only genuine GHSA-advisory-bearing findings (ignoring npm's transitive "depends on vulnerable versions of X" fan-out, which reports 15 "high" package entries for this single root cause), failing on any HIGH/CRITICAL advisory not exactly matched -- by advisory URL, package, **version, and dependency path** -- against `scripts/security/npm-audit-exceptions.json` (`SEC-EXC-001`/`SEC-EXC-002`, expiring 2026-09-14). The dependency path is not trusted from the exception record itself: it is independently re-derived every run from real `npm ls <pkg> --json --all` structural data (`findAllDependencyPaths`), so drift to a different path, or the same package becoming reachable via an *additional* unexpected path, both fail the gate rather than silently continuing to suppress it. **Two explicit modes** (CC-04N-S1 hardening): `npm run security:audit` (NORMAL mode -- the only mode CI ever calls) permits ONLY `status: "accepted"` exceptions; a `status: "proposed"` exception fails NORMAL mode with `EXCEPTION_NOT_ACCEPTED`, so a not-yet-approved exception can never make mandatory CI green. `npm run security:audit:review` (REVIEW mode, `--allow-proposed`, never called by CI) additionally permits `status: "proposed"` exceptions, so a proposed exception can be proven correct ahead of approval -- every other protection (unexpired, correct version, correct path, no unexpected new finding, not stale) remains fully active in both modes. Unit-tested with 15 deterministic fixtures (`scripts/security/check-npm-audit.test.ts`): accepted-exact-match passes; proposed-in-normal-mode fails; the same proposed exception in review mode passes; unexpected HIGH/CRITICAL fails; expiry fails; version mismatch fails; dependency-path drift fails; an additional unexpected path fails; a stale exception is flagged rather than silently tolerated. This is dev/build-toolchain-only exposure (Metro image-dimension parsing during local/CI bundling); no shipped learner-app code imports or executes `image-size`. **Current live state (2026-08-15)**: `SEC-EXC-001`/`SEC-EXC-002` were explicitly **accepted** by Product Owner / Project Architect on 2026-08-15, through the exception's original expiry of **2026-09-14** (not extended). `npm run security:audit` (NORMAL mode, what CI runs) now correctly PASSes, both findings COVERED by the accepted exceptions; `npm run security:audit:review` also still passes. Raw `npm audit --json` is unaffected and unfiltered by this acceptance -- it still reports the full 15-entry HIGH fan-out from the same root cause; only the governed gate applies the exception, and only to these exact two advisory/package/version/path tuples. **The underlying vulnerability is NOT resolved and image-size@1.2.1 is unchanged** -- this row records a temporarily *accepted risk*, not a fix. | **ACCEPTED TEMPORARY EXCEPTION** | Accepted by: `product-owner / project-architect`. Accepted on: 2026-08-15. Expires: **2026-09-14** (mandatory re-review at/before this date; not extended by this acceptance). Scope: exactly `image-size@1.2.1` via `expo@57.0.14 > @expo/metro@56.0.0 > metro@0.84.4 > image-size@1.2.1`, advisories `GHSA-w3rx-r6r6-pgpr` and `GHSA-5p2g-fcmc-qvqq`, development/build-toolchain only -- this acceptance does **not** extend to any other HIGH/CRITICAL finding, another `image-size` version, another dependency path, an *additional* path alongside this one, a changed advisory, or any finding after expiry; the gate fails automatically and re-opens governance review the moment any of those drift. `npm audit --audit-level=high` alone (without this governed gate) would still fail -- that is expected and intentional; raw audit transparency is deliberately preserved, not hidden. |

Rules: add granular rows as components are implemented; cite exact requirements where practical; provider-managed N/A requires rationale; code existence alone is not VERIFIED; evidence should point to tests/config/review; exceptions require rationale; review before external beta and major security-boundary changes.
